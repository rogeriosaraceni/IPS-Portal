<section class="hero">
    <h1 class="mb-0 text-white title">What are you looking for ?</h1>
    <div class="image image-overlay" style="background-image:url(/assets/images/hero.png)"></div>
</section>

<style is:inline>
    .hero {

        position: relative;
        margin: 0 auto;
        padding: 2.25rem 0;
        padding-top: 14.5rem;
        overflow: hidden;
        width: 100%;
    }
    .image {
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        background-size: cover;
        background-position: center;
        z-index: -1;
    }
    .image[class*="-overlay"]::after {
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        content: "";
        background: rgba(0, 0, 0, 0.45);
    }
    .hero .title{
        position: absolute;
        top: 35%;
        left: 13%;
    }
</style>
